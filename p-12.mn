var c1 = new Car("Mazda", "MPV5");
public class Car
{
    public string Brand { get; set; }
    public string Model { get; set; }
    public int Speed { get; set; }
    public Car(string brand, string model) : this(brand, "Неизвестно", 0)
    {

    }
    public Car(string brand, string model, int speed)
    {
        Brand = brand;
        Model = model;
        Speed = speed;
    }
}